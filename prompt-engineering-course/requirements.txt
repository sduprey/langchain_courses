# Verified to install AND import together during the course build (Python 3.12).
# These are HARD pins on purpose: a "latest" install is how a course breaks
# live in class. Bump deliberately, then re-run the eval suite.
python-dotenv==1.2.3
pydantic==2.13.5
tiktoken==0.14.0
numpy==2.5.3
openai==3.8.0        # Track B only (tools, safety labs)
ollama==0.6.2        # Track A (requires the local Ollama app running)

# Jupyter — not part of the import-verify above; pin once you pick a version:
notebook==7.2.2
